name: fni_gestao_frotas
description: FNI Gestao de Frotas
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'
dependencies:
  flutter:
    sdk: flutter
  # Fase FLT-1 — migração da autenticação/dados da API Python própria pro
  # Supabase direto (mesmo backend da web: nedthbeekvwzcjrhsghp), com Auth +
  # RLS + as mesmas RPCs já usadas em src/lib/*.ts do app Next.js.
  supabase_flutter: ^2.5.6
  dio: ^5.4.0
  google_sign_in: ^6.2.1
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  fl_chart: ^0.67.0
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.3
  equatable: ^2.0.5
  url_launcher: ^6.3.2
  file_picker: ^11.0.2
  web_socket_channel: ^3.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
flutter:
  uses-material-design: true
  assets:
    - assets/logo_fni.png
